CHALLENGES FACING
-- having to store users task into the database and fetching them back on the board
-- keeping track of the task order exactly the way it was added to the board in real time,
and knowing the position of each board
-- persisting the user data
-- dynamically expanding the card container(area:height) when moving cards

This is the client side of the kanban board for ALX project